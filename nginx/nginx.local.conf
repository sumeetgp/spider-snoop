events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    
    # Logging
    access_log  /var/log/nginx/access.log;
    error_log   /var/log/nginx/error.log;

    # Docker DNS Resolver
    resolver 127.0.0.11 valid=30s;

    # Optimization
    sendfile        on;
    keepalive_timeout  65;
    client_max_body_size 50M; # Updated to match 50MB app limit
    gzip  on;
    gzip_types text/plain application/json text/css text/javascript application/javascript;

    # Main Server (HTTP only for dev)
    server {
        listen 80;
        server_name spidercob.com www.spidercob.com localhost;

        set $upstream_endpoint http://api:8000;

        # Health check
        location /health {
            proxy_pass $upstream_endpoint$request_uri;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Static files
        location /static/ {
            proxy_pass $upstream_endpoint$request_uri;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # API Endpoints
        location /api/ {
            proxy_pass $upstream_endpoint$request_uri;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Main App
        location / {
            proxy_pass $upstream_endpoint;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket support
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}
